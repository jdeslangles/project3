<%= form_for(@marker) do |f| %>
  <% if @marker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@marker.errors.count, "error") %> prohibited this marker from being saved:</h2>

      <ul>
      <% @marker.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, class:"input_field" %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, :size => "30x5", class:"input_field" %>
  </div>
  <div class="field">
    <%= f.label :address %><br />
    <%= f.text_field :address, class:"input_field" %>
  </div>
  <div class="field links">
    <%= f.label :photo %><br>
    <%= f.file_field :photo, class: "centered", :onchange =>"validateFiles(this);", :data => { :max_file_size => 1.megabyte }%><br/><br>
</div>
  <div class="actions">
    <br>
    <%= f.submit "Update", class:"btn btn-sm btn-primary"  %>
  </div>
<% end %>
